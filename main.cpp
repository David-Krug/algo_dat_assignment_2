#include "bst.hpp"

/*
Pseudocode printBST(node X) {
	if node[left] == existant:
		nodeX -> node[left];		//Verbindung zw node X und linkem Kindknoten
		printBST(node[left]);		//Aufruf für linkes Kind von X (und all deren Kinder)
	if node[right] == existant:
		nodeX -> node[right];		//Verbindung zw node X und linkem Kindknoten
		printBST(node[right]);		//Rekursiver Aufruf
}
*/

int main()
{
  //printBST()
  return 1;
}